<%= render 'layouts/header', title: "The Place to Blub" %>


<% if user_signed_in? %>

  <%= link_to 'Blub a New Blub', new_blub_path, id: "new_blub_link", remote: true %>

<% end %>

  <h2>All Blubs</h2>

  <div id="blubs_feed">
    <%= render @blubs.reverse %>
  </div>
